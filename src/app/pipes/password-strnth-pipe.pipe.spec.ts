import { PasswordStrnthPipePipe } from './password-strnth-pipe.pipe';

describe('PasswordStrnthPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PasswordStrnthPipePipe();
    expect(pipe).toBeTruthy();
  });
});
