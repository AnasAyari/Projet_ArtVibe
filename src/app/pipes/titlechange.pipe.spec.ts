import { TitlechangePipe } from './titlechange.pipe';

describe('TitlechangePipe', () => {
  it('create an instance', () => {
    const pipe = new TitlechangePipe();
    expect(pipe).toBeTruthy();
  });
});
